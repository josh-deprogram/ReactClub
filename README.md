# ReactClub
ReactNative embeded into Native App _

Native TabApp shell, with a ReactNativeView loaded into the first View

_MapBoxGL_ used on the RN screen

Get Started

`yarn install`

`yarn start`

```
cd ios
pod install
```

`react-native run-ios` or boot up from Xcode
